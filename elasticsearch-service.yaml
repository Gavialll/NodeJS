apiVersion: v1
kind: Service
metadata:
  name: elasticsearch
spec:
  selector:
    app: elasticsearch
  ports:
    - port: 9200
      targetPort: 9200
#      nodePort: 30001 # Розкоментувати якщо буде потрібно підключитися за меж клсатеру
#  type: NodePort # Розкоментувати якщо буде потрібно підключитися за меж клсатеру


